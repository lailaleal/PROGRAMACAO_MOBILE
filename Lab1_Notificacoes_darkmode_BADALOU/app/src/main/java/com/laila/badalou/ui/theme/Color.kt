package com.laila.badalou.ui.theme

import androidx.compose.ui.graphics.Color

// ─────────────────────────────────────
// CORES PRINCIPAIS — Tema Claro
// ─────────────────────────────────────
val AmbarPrimary = Color(0xFFFF9800)      // Laranja âmbar vibrante
val AmbarLight = Color(0xFFFFB74D)        // Âmbar claro
val AmbarDark = Color(0xFFF57C00)         // Âmbar escuro

val GreenSuccess = Color(0xFF4CAF50)      // Verde conquista
val GreenDark = Color(0xFF388E3C)         // Verde escuro

val BackgroundLight = Color(0xFFF9F9F9)   // Fundo claro
val SurfaceLight = Color(0xFFFFFFFF)      // Superfície branca
val TextPrimary = Color(0xFF333333)       // Texto principal
val TextSecondary = Color(0xFF757575)     // Texto secundário

// ─────────────────────────────────────
// CORES PRINCIPAIS — Tema Escuro
// ─────────────────────────────────────
val BackgroundDark = Color(0xFF1A1A1A)    // Fundo escuro
val SurfaceDark = Color(0xFF2C2C2C)       // Superfície escura
val TextPrimaryDark = Color(0xFFF5F5F5)   // Texto claro
val TextSecondaryDark = Color(0xFFAAAAAA) // Texto secundário escuro

// ─────────────────────────────────────
// CORES DAS CATEGORIAS
// ─────────────────────────────────────
val CategoriaEstudar = Color(0xFF3F51B5)      // Indigo — Estudar
val CategoriaBeberAgua = Color(0xFF00BCD4)    // Ciano — Beber Água
val CategoriaExercicio = Color(0xFFFF9800)    // Âmbar — Exercício
val CategoriaMedicamento = Color(0xFFE53935)  // Vermelho — Medicamento
val CategoriaCompromisso = Color(0xFF8E24AA)  // Roxo — Compromisso
val CategoriaOutros = Color(0xFF546E7A)       // Cinza azulado — Outros